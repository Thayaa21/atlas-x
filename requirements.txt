pandas
numpy
scikit-learn
xgboost
streamlit
shap
matplotlib
python-dotenv
openai
pyarrow
fastapi

# Production / serving
uvicorn[standard]
orjson
httpx

# Messaging / streaming
aiokafka

# Graph DB
neo4j

# Caching / state
redis

# Postgres (async driver)
asyncpg

# Metrics
prometheus-fastapi-instrumentator
psutil

# RL / Federated learning
torch
flwr

# Training utilities
joblib
aiohttp>=3.9

# Feature 7: Dual LLM (Qwen via Ollama + OpenAI)
# httpx already listed above (used for async Ollama calls)
# openai already listed above

# Feature 6: pgvector similarity search
# pgvector extension is installed in PostgreSQL (see docker/Dockerfile.postgres)
# Python client uses asyncpg (already listed) with raw SQL

# Feature 9: Data augmentation
# numpy already listed above
